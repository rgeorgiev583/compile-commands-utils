# vim-compiledb-pathgen
A Vim/Neovim plugin which scans a JSON Compilation Database file and generates a list of C/C++/Objective-C/Objective-C++ include paths to be appended to the editor search path list.
